meta:
  description: SQL repair for generic BIRD-style DB.
template: 'Fix the SQL query so it executes successfully against the database.

  Return only corrected SQL, no explanation.


  Broken SQL:

  {broken_sql}


  Execution error:

  {error_text}

  '
